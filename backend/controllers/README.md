# Controllers Directory

This directory contains the request handler logic. Controllers process incoming requests, interact with models (data layer), and return responses.

Example:
- `userController.js`: Functions like `registerUser`, `loginUser`, and `getUserProfile`.
