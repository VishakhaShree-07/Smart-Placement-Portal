# Models Directory

This directory contains schemas and database models (e.g. Mongoose schemas for MongoDB) that define the shape and validation of data stored in the database.

Example:
- `User.js`: Schema definitions for username, email, password, roles, and profile details.
