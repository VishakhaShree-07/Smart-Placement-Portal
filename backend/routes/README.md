# Routes Directory

This directory is used for defining backend API endpoints and routing requests to their corresponding controllers.

Example:
- `userRoutes.js`: Routing for user registration, login, etc.
- `placementRoutes.js`: Routing for placement preparation resource endpoints.
