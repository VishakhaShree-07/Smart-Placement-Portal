# Config Directory

This directory is used for housing configuration settings for the backend, such as database connection scripts, external API setup, and key constants.

Example:
- `db.js`: Script to connect to MongoDB using Mongoose.
